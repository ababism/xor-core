version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: ./deploy/Dockerfile
      target: prod
    image: payments:${PAYMENTS_VERSION:-latest}
    restart: always
    env_file:
    - .env
    environment:
      PAYMENTS_HOST: 0.0.0.0
